import { Options, Vue } from 'vue-class-component';

@Options({
    name: 'Home',
    components: {
    }
})
export default class Home extends Vue {
    public mounted() {
        // add test here :-)
    }
}
